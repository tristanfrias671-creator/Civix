services:
  - type: web
    name: civix-api
    runtime: node
    region: singapore
    plan: free
    repo: https://github.com/tristanfrias671-creator/Civix.git
    branch: main
    rootDir: server
    buildCommand: npm ci --include=dev && npx prisma generate
    startCommand: node start.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: MIGRATION_DATABASE_URL
        sync: false
      - key: CLIENT_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
